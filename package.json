{
  "name": "analytics-usdt",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ analytics-usdt",
    "create-local": "graph create --node http://localhost:8020/ analytics-usdt",
    "remove-local": "graph remove --node http://localhost:8020/ analytics-usdt",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 analytics-usdt",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.79.0",
    "@graphprotocol/graph-ts": "0.32.0"
  },
  "devDependencies": { "matchstick-as": "0.5.0" }
}
